import { View, Text } from 'react-native';

export default function ExploreScreenTest() {
    return (
        <View style ={{justifyContent: 'center', alignItems: 'center', flex:1}}>
            <Text>Explore Screen</Text>
        </View>
    );
}