import { View, Text } from 'react-native';

export default function MoreScreenTest() {
    return (
        <View style ={{justifyContent: 'center', alignItems: 'center', flex:1}}>
            <Text>More Screen</Text>
        </View>
    );
}